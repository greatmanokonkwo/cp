#include<bits/stdc++.h>

using namespace std;

int rank_hand()

int main(){
	freopen("poker.txt","r",stdin);

	vector<string> hand1 (5); vector<string> hand2 (5);
	string card; int i;
	while(!cin.eof()){
		for (i=0;i<5;i++) cin >> hand1[i];
		for (i=0;i<5;i++) cin >> hand2[i];

		
	}
}
